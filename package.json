{
  "name": "annico",
  "version": "1.0.0",
  "private": true,
  "description": "Wedding information website for Anne & Nico",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "dev:full": "concurrently -n backend,frontend -c blue,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm run start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
